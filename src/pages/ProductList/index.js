import React from 'react';
import ProductListPresentation from '../../components/ProductList'

const index = () => {
    return (
        <div>
            <ProductListPresentation/>
        </div>
    );
}

export default index;
